state_api:
  mode: event_sourced

entities:
  - PhaseRun
  - RoleTask
  - RoleAttempt
  - RoleOutput
  - AuditEvent

rules:
  - append_only: true
  - no_update_allowed: true
  - idempotency_required: true

write_policy:
  source_of_truth: StateAPI
  flowise: emitter_only