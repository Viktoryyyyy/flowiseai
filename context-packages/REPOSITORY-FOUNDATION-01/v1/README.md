# REPOSITORY-FOUNDATION-01 immutable input package

This context-only branch stores the verified input bundle for PM L2.

Use the final full commit SHA and exact path to manifest.yaml. Do not use the branch tip as an immutable locator.
